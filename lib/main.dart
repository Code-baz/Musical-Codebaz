import 'package:flutter/material.dart';

void main() => runApp(CodeBazMusical());

class CodeBazMusical extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: SafeArea(
          child: Container(),
        ),
      ),
    );
  }
}
